#include <algorithm>
#include <iostream>
#include <cstdio>

const int MAXN = 5e5;
int value[MAXN];

int main()
{
	int n, q;
	std::cin >> n >> q;

	for (int i = 0; i < n; i++)
		std::cin >> value[i];
	return 0;
}