#include <iostream>
using namespace std;
int main()
{
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < q; i++)
    {
        int type, x, y;
        cin >> type >> x >> y;
        
    }
    return 0;
}