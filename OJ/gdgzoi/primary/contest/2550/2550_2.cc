#include <cstddef>
#include <iostream>

int main()
{
    std::string str;
    while (std::cin >> str)
    {
        for (std::size_t i = 0; i < str.size(); i++)
        {
            if ()
        }
    }
    return 0;
}